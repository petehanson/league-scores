<h1>Enter Scores</h1>
<p class="lead">Use this form to record each set's score for your court.</p>
{{#each leagues}}
    <div class="col mb-4">
        <a href="/s/{{key}}" class="btn btn-primary btn-lg w-100">{{value}}</a>
    </div>
{{/each}}